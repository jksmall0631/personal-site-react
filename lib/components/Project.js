import React from 'react';

const Project = ({image, title, description}) => {
  <img src={require({image})} width='600px' alt="{alt}" />
  <h1>{title}</h1>
  <p>{description}</p>
}

export default Project;
